# CLAUDE.md

gwilym.ca is currently in **placeholder mode** — `main` deploys a single page with a Welsh dragon on a near-black background.

The previous full Astro + Svelte/React build is preserved on the `pre-placeholder` branch. Restore from there when work resumes.
